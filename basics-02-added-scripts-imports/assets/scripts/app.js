const defaultResult = 0;
let currentResult = defaultResult;
currentResult += 10;

let calculationDescription = `${'heheheheh'}(` + currentResult + "0 + 10) * 3 / 2 - 1";
outputResult(currentResult, calculationDescription);
